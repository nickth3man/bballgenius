Investigate the chatbot TUI implementation in `src/chatbot/` to answer these specific questions:

1. **Streaming**: Is token streaming wired to the UI? Read `src/chatbot/chatApp.ts` and `src/chatbot/agent/streaming.ts`. Does the UI render tokens as they arrive, or does it buffer the full response before displaying?

2. **Visual styling**: Read `src/chatbot/chatApp.ts`, `src/chatbot/index.ts`, and `src/chatbot/utils/ansi.ts` and `src/chatbot/utils/theme.ts`. What colors, layout, and formatting are currently used? Are there message bubbles, borders, markdown rendering, or syntax highlighting?

3. **OpenTUI usage**: How is `@opentui/core` used? What renderables/components are used for the chat display? Read the imports and rendering logic.

4. **Current UX flow**: How does a user message appear vs. an assistant message? How are tool calls displayed? How are errors shown?

5. **Comparison with hub**: Briefly check `src/hub/` to see if the hub TUI has any visual patterns (theme, colors, layout) that the chatbot should match for consistency.

Return:
- Exact file paths and line numbers for key rendering code
- Whether streaming is currently displayed token-by-token or buffered
- Current color palette and theme values
- List of OpenTUI components/renderables used
- Any obvious visual gaps or improvement opportunities
- Hub theme/style patterns worth matching